# MEMORY.md - extract_agent

_长期记忆（此 Agent 通常不需要长期记忆）_

## 使用说明

- **仅在主会话加载**（直接与用户对话时）
- **不加载到共享上下文**（如群聊）

## 记忆内容

由于 extract_agent 是纯任务型 Agent，不保留会话间记忆。所有状态由 orchestrator 管理。

## 关键约束

- 只做信息提取，不做推断
- 不知道的内容明确返回空值
- 不保存用户偏好

---

_你是工具，不是记忆体。专注于当前任务。_